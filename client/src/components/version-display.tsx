import { useState, useEffect } from 'react';

interface VersionInfo {
  version: string;
  buildDate: string;
  lastUpdate: string;
}

export function VersionDisplay() {
  return null; // Removed version display as requested
}